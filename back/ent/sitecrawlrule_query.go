// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/haikara-dev/haikara/ent/predicate"
	"github.com/haikara-dev/haikara/ent/site"
	"github.com/haikara-dev/haikara/ent/sitecrawlrule"
)

// SiteCrawlRuleQuery is the builder for querying SiteCrawlRule entities.
type SiteCrawlRuleQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.SiteCrawlRule
	withSite   *SiteQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SiteCrawlRuleQuery builder.
func (scrq *SiteCrawlRuleQuery) Where(ps ...predicate.SiteCrawlRule) *SiteCrawlRuleQuery {
	scrq.predicates = append(scrq.predicates, ps...)
	return scrq
}

// Limit adds a limit step to the query.
func (scrq *SiteCrawlRuleQuery) Limit(limit int) *SiteCrawlRuleQuery {
	scrq.limit = &limit
	return scrq
}

// Offset adds an offset step to the query.
func (scrq *SiteCrawlRuleQuery) Offset(offset int) *SiteCrawlRuleQuery {
	scrq.offset = &offset
	return scrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (scrq *SiteCrawlRuleQuery) Unique(unique bool) *SiteCrawlRuleQuery {
	scrq.unique = &unique
	return scrq
}

// Order adds an order step to the query.
func (scrq *SiteCrawlRuleQuery) Order(o ...OrderFunc) *SiteCrawlRuleQuery {
	scrq.order = append(scrq.order, o...)
	return scrq
}

// QuerySite chains the current query on the "site" edge.
func (scrq *SiteCrawlRuleQuery) QuerySite() *SiteQuery {
	query := &SiteQuery{config: scrq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := scrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := scrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sitecrawlrule.Table, sitecrawlrule.FieldID, selector),
			sqlgraph.To(site.Table, site.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, sitecrawlrule.SiteTable, sitecrawlrule.SiteColumn),
		)
		fromU = sqlgraph.SetNeighbors(scrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SiteCrawlRule entity from the query.
// Returns a *NotFoundError when no SiteCrawlRule was found.
func (scrq *SiteCrawlRuleQuery) First(ctx context.Context) (*SiteCrawlRule, error) {
	nodes, err := scrq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{sitecrawlrule.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (scrq *SiteCrawlRuleQuery) FirstX(ctx context.Context) *SiteCrawlRule {
	node, err := scrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SiteCrawlRule ID from the query.
// Returns a *NotFoundError when no SiteCrawlRule ID was found.
func (scrq *SiteCrawlRuleQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = scrq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{sitecrawlrule.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (scrq *SiteCrawlRuleQuery) FirstIDX(ctx context.Context) int {
	id, err := scrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SiteCrawlRule entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SiteCrawlRule entity is found.
// Returns a *NotFoundError when no SiteCrawlRule entities are found.
func (scrq *SiteCrawlRuleQuery) Only(ctx context.Context) (*SiteCrawlRule, error) {
	nodes, err := scrq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{sitecrawlrule.Label}
	default:
		return nil, &NotSingularError{sitecrawlrule.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (scrq *SiteCrawlRuleQuery) OnlyX(ctx context.Context) *SiteCrawlRule {
	node, err := scrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SiteCrawlRule ID in the query.
// Returns a *NotSingularError when more than one SiteCrawlRule ID is found.
// Returns a *NotFoundError when no entities are found.
func (scrq *SiteCrawlRuleQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = scrq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{sitecrawlrule.Label}
	default:
		err = &NotSingularError{sitecrawlrule.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (scrq *SiteCrawlRuleQuery) OnlyIDX(ctx context.Context) int {
	id, err := scrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SiteCrawlRules.
func (scrq *SiteCrawlRuleQuery) All(ctx context.Context) ([]*SiteCrawlRule, error) {
	if err := scrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return scrq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (scrq *SiteCrawlRuleQuery) AllX(ctx context.Context) []*SiteCrawlRule {
	nodes, err := scrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SiteCrawlRule IDs.
func (scrq *SiteCrawlRuleQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := scrq.Select(sitecrawlrule.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (scrq *SiteCrawlRuleQuery) IDsX(ctx context.Context) []int {
	ids, err := scrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (scrq *SiteCrawlRuleQuery) Count(ctx context.Context) (int, error) {
	if err := scrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return scrq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (scrq *SiteCrawlRuleQuery) CountX(ctx context.Context) int {
	count, err := scrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (scrq *SiteCrawlRuleQuery) Exist(ctx context.Context) (bool, error) {
	if err := scrq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return scrq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (scrq *SiteCrawlRuleQuery) ExistX(ctx context.Context) bool {
	exist, err := scrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SiteCrawlRuleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (scrq *SiteCrawlRuleQuery) Clone() *SiteCrawlRuleQuery {
	if scrq == nil {
		return nil
	}
	return &SiteCrawlRuleQuery{
		config:     scrq.config,
		limit:      scrq.limit,
		offset:     scrq.offset,
		order:      append([]OrderFunc{}, scrq.order...),
		predicates: append([]predicate.SiteCrawlRule{}, scrq.predicates...),
		withSite:   scrq.withSite.Clone(),
		// clone intermediate query.
		sql:    scrq.sql.Clone(),
		path:   scrq.path,
		unique: scrq.unique,
	}
}

// WithSite tells the query-builder to eager-load the nodes that are connected to
// the "site" edge. The optional arguments are used to configure the query builder of the edge.
func (scrq *SiteCrawlRuleQuery) WithSite(opts ...func(*SiteQuery)) *SiteCrawlRuleQuery {
	query := &SiteQuery{config: scrq.config}
	for _, opt := range opts {
		opt(query)
	}
	scrq.withSite = query
	return scrq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SiteCrawlRule.Query().
//		GroupBy(sitecrawlrule.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (scrq *SiteCrawlRuleQuery) GroupBy(field string, fields ...string) *SiteCrawlRuleGroupBy {
	grbuild := &SiteCrawlRuleGroupBy{config: scrq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := scrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return scrq.sqlQuery(ctx), nil
	}
	grbuild.label = sitecrawlrule.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.SiteCrawlRule.Query().
//		Select(sitecrawlrule.FieldCreatedAt).
//		Scan(ctx, &v)
func (scrq *SiteCrawlRuleQuery) Select(fields ...string) *SiteCrawlRuleSelect {
	scrq.fields = append(scrq.fields, fields...)
	selbuild := &SiteCrawlRuleSelect{SiteCrawlRuleQuery: scrq}
	selbuild.label = sitecrawlrule.Label
	selbuild.flds, selbuild.scan = &scrq.fields, selbuild.Scan
	return selbuild
}

func (scrq *SiteCrawlRuleQuery) prepareQuery(ctx context.Context) error {
	for _, f := range scrq.fields {
		if !sitecrawlrule.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if scrq.path != nil {
		prev, err := scrq.path(ctx)
		if err != nil {
			return err
		}
		scrq.sql = prev
	}
	return nil
}

func (scrq *SiteCrawlRuleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SiteCrawlRule, error) {
	var (
		nodes       = []*SiteCrawlRule{}
		withFKs     = scrq.withFKs
		_spec       = scrq.querySpec()
		loadedTypes = [1]bool{
			scrq.withSite != nil,
		}
	)
	if scrq.withSite != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, sitecrawlrule.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*SiteCrawlRule).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &SiteCrawlRule{config: scrq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, scrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := scrq.withSite; query != nil {
		if err := scrq.loadSite(ctx, query, nodes, nil,
			func(n *SiteCrawlRule, e *Site) { n.Edges.Site = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (scrq *SiteCrawlRuleQuery) loadSite(ctx context.Context, query *SiteQuery, nodes []*SiteCrawlRule, init func(*SiteCrawlRule), assign func(*SiteCrawlRule, *Site)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*SiteCrawlRule)
	for i := range nodes {
		if nodes[i].site_site_crawl_rule == nil {
			continue
		}
		fk := *nodes[i].site_site_crawl_rule
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	query.Where(site.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "site_site_crawl_rule" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (scrq *SiteCrawlRuleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := scrq.querySpec()
	_spec.Node.Columns = scrq.fields
	if len(scrq.fields) > 0 {
		_spec.Unique = scrq.unique != nil && *scrq.unique
	}
	return sqlgraph.CountNodes(ctx, scrq.driver, _spec)
}

func (scrq *SiteCrawlRuleQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := scrq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (scrq *SiteCrawlRuleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   sitecrawlrule.Table,
			Columns: sitecrawlrule.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: sitecrawlrule.FieldID,
			},
		},
		From:   scrq.sql,
		Unique: true,
	}
	if unique := scrq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := scrq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, sitecrawlrule.FieldID)
		for i := range fields {
			if fields[i] != sitecrawlrule.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := scrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := scrq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := scrq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := scrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (scrq *SiteCrawlRuleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(scrq.driver.Dialect())
	t1 := builder.Table(sitecrawlrule.Table)
	columns := scrq.fields
	if len(columns) == 0 {
		columns = sitecrawlrule.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if scrq.sql != nil {
		selector = scrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if scrq.unique != nil && *scrq.unique {
		selector.Distinct()
	}
	for _, p := range scrq.predicates {
		p(selector)
	}
	for _, p := range scrq.order {
		p(selector)
	}
	if offset := scrq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := scrq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SiteCrawlRuleGroupBy is the group-by builder for SiteCrawlRule entities.
type SiteCrawlRuleGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (scrgb *SiteCrawlRuleGroupBy) Aggregate(fns ...AggregateFunc) *SiteCrawlRuleGroupBy {
	scrgb.fns = append(scrgb.fns, fns...)
	return scrgb
}

// Scan applies the group-by query and scans the result into the given value.
func (scrgb *SiteCrawlRuleGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := scrgb.path(ctx)
	if err != nil {
		return err
	}
	scrgb.sql = query
	return scrgb.sqlScan(ctx, v)
}

func (scrgb *SiteCrawlRuleGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range scrgb.fields {
		if !sitecrawlrule.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := scrgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := scrgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (scrgb *SiteCrawlRuleGroupBy) sqlQuery() *sql.Selector {
	selector := scrgb.sql.Select()
	aggregation := make([]string, 0, len(scrgb.fns))
	for _, fn := range scrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(scrgb.fields)+len(scrgb.fns))
		for _, f := range scrgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(scrgb.fields...)...)
}

// SiteCrawlRuleSelect is the builder for selecting fields of SiteCrawlRule entities.
type SiteCrawlRuleSelect struct {
	*SiteCrawlRuleQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (scrs *SiteCrawlRuleSelect) Scan(ctx context.Context, v interface{}) error {
	if err := scrs.prepareQuery(ctx); err != nil {
		return err
	}
	scrs.sql = scrs.SiteCrawlRuleQuery.sqlQuery(ctx)
	return scrs.sqlScan(ctx, v)
}

func (scrs *SiteCrawlRuleSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := scrs.sql.Query()
	if err := scrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
